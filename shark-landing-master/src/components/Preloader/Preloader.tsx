const Preloader = () => {
  return (
    <div className="bg-rgba([0,0,0,.5]) fixed inset-0 w-full h-full flex items-center justify-center">
      <span className="text-white font-bold text-[16px]">LOADING...</span>
    </div>
  );
};

export default Preloader;
